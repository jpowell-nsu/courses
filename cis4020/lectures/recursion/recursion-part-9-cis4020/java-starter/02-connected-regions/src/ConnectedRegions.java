public class ConnectedRegions {
    // Returns the number of land ('L') cells connected to (row, col) by
    // moving up, down, left, or right through other land cells.
    //
    // TODO: implement this method, including the base cases we worked out
    // as a class.
    public static int regionSize(char[][] grid, boolean[][] visited, int row, int col) {
        return 0;
    }
}
