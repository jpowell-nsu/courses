public class CoinChange {
    // Returns the number of ways to make amount using the given coins,
    // starting the search at coins[index]. Each coin may be used any
    // number of times, and the order coins are picked in does not matter.
    //
    // TODO: implement this method, including the base cases we worked out
    // as a class.
    public static int countChange(int amount, int[] coins, int index) {
        return 0;
    }
}
